{% macro printProducts(products, emptyMessage) %}

	{% for product in products %}
		<div class="span4">
			<h2>{{ product.getName() }}</h2>

      <div class="product-img">
        <img src="../assets/images/{{ product.getimagePath()}}" alt="{{ product.getName() }}",
        </div>
        
            {# <div class="price">
            {{ (random()/100)|number_format(1)}}

            {% if random(10) < 5 %}
              <div>Wow, this is some cheap swag!</div>
              {% endif %}
            </div> #}
		</div>
    {%if loop.index is divisibleby(3)%}
    </div><div class="row">
    {% endif %}
    {% else %}
    <div class="alart alter-error span12">
      Oh No! We're all out of super-awesome penguin clothes!
      </div>
	{% endfor %}

{% endmacro %}